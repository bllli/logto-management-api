# SigningKeyAlgorithmRequest

The signing key algorithm the new generated private key is using.

Only applicable when `keyType` is `private-keys`.


## Values

| Name                            | Value                           |
| ------------------------------- | ------------------------------- |
| `SigningKeyAlgorithmRequestRsa` | RSA                             |
| `SigningKeyAlgorithmRequestEc`  | EC                              |